name: Playwright Contact Us Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  contact_us_tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.18.0

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install

      - name: Run Contact Us test
        run: npx playwright test tests/about_page/contact_us.spec.js --reporter=list,allure-playwright --retries=2

      - name: Generate Allure Report
        run: npx allure generate allure-results --clean -o allure-report

      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report








  # playground-main:
  #   name: Run Main page tests (Playwright)
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20.18.0
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Install Playwright Browsers
  #       run: npx playwright install --with-deps
  #
  #     - name: Run Main tests with Allure
  #       run: |
  #         npm run test:main -- --reporter=list,allure-playwright
  #
  #     - name: Upload Allure results (Main)
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: allure-results-main
  #         path: allure-results
  #         retention-days: 30

