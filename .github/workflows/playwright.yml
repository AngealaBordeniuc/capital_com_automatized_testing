name: Investor Relations Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  client-vulnerability:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Client Vulnerability test only
        run: npx playwright test tests/about_page/investor_relations.spec.js



# name: Playwright About Tests with Allure

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   about-tests:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 20.18.0

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install

#       - name: Install Allure CLI
#         run: npm install -g allure-commandline

#       - name: Run Playwright About tests
#         run: |
#           npx playwright test tests/about_page --reporter=list,allure-playwright --retries=2

#       - name: Generate Allure Report in daily folder
#         run: |
#           DATE=$(date +'%Y-%m-%d')
#           npx allure generate allure-results --clean -o allure-report/$DATE
#           ls -la allure-report/$DATE

#       - name: Deploy Allure Report to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GH_PAT }}   # Personal Access Token cu Read & Write
#           publish_dir: ./allure-report
#           publish_branch: gh-pages


# name: Playwright About Tests with Allure

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   about-tests:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 20.18.0

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install

#       - name: Install Allure CLI
#         run: npm install -g allure-commandline

#       - name: Run Playwright About tests
#         run: |
#           npx playwright test tests/about_page --reporter=list,allure-playwright --retries=2

#       - name: Generate Allure Report
#         run: |
#           npx allure generate allure-results --clean -o allure-report

#       # 🔹 redenumim folderul raportului cu data curenta
#       - name: Rename Allure folder by date
#         run: |
#           DATE=$(date +'%Y-%m-%d')
#           mv allure-report allure-report-$DATE
#           echo "ALLURE_DIR=allure-report-$DATE" >> $GITHUB_ENV

#       - name: Deploy Allure Report to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GH_PAT }}
#           publish_dir: ${{ env.ALLURE_DIR }}
          # publish_branch: gh-pages


# name: Playwright About Tests with Allure

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   about-tests:
#     runs-on: ubuntu-latest
#     timeout-minutes: 60

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20.18.0

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install --with-deps

#       - name: Install Allure CLI
#         run: npm install -g allure-commandline

#       - name: Run Playwright About tests
#         run: npx playwright test tests/about_page --reporter=list,allure-playwright --retries=2

#       - name: Generate Allure Report
#         if: always()
#         run: |
#           npx allure generate allure-results --clean -o allure-report

#       - name: Deploy Allure Report to GitHub Pages
#         if: always()
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           personal_token: ${{ secrets.GH_PAT }}
#           publish_dir: ./allure-report
#           publish_branch: gh-pages
#           force_orphan: true














  # playground-main:
  #   name: Run Main page tests (Playwright)
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20.18.0
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Install Playwright Browsers
  #       run: npx playwright install --with-deps
  #
  #     - name: Run Main tests with Allure
  #       run: |
  #         npm run test:main -- --reporter=list,allure-playwright
  #
  #     - name: Upload Allure results (Main)
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: allure-results-main
  #         path: allure-results
  #         retention-days: 30

